{{!< default}}

{{! The comment above "< default" means - insert everything in this file into
    the {body} of the default.hbs template, which contains our header/footer. }}

{{! Everything inside the #post tags pulls data from the post }}
{{#post}}
<div class="grid-block clearfix">{{> "masthead"}}</div>

<main class="content grid-block clearfix" role="main">
    <div class="grid-row">
        <article class="{{post_class}} post grid-column actualpostcontentarea" style="margin-bottom:60px;">
            <header class="post-header">
                <h1 class="post-title">{{title}}</h1>
                <p class="post-date"><time datetime="{{date format='YYYY-MM-DD'}}">{{date format="MMM Do YYYY"}}</time> by {{author}} {{#if tags}} {{tags prefix=" in "}} {{/if}}</p>
                {{#if image}}
                <img  src="{{image}}" />
                {{/if}}
            </header>
            <section class="post-content grid-column eight-wide-column">
                {{content}}
            </section>
            <div class="sidebar grid-column three-wide-column">
              <!-- <h4>Sponsor The Pulse</h4>
              <div class="sponsorbox prologuead"></div>
              <p>Karl is a fine young man, sometimes He Programs, but mostly he’s just lazy and writes stuff for this blog. I hope that you like that.</p> -->
              <!-- Alright So I think that this spot will hold a default message that will be replaced from time to time. When new sponsorships arise. -->
              <div class="sponsor-wrapper"> <!-- Wraps the sponsorship so we can replace it later. -->
                <h4>Sponsor The Pulse</h4>
                <div class="sponsorbox pulsead"></div>
                <p>A Podcast about a Freelancer trying to run his business without running it into the ground. New episodes every Tuesday. <br><br>Limited Sponsorships available, email <a href="mailto:me@kow.fm?subject=I'd like to Sponsor The Pulse">me@kow.fm</a> for more details</p>
              </div>
              {{#author}}
                <h4>About the Author:</h4>
                {{#if image}}
                <div class="authoravatarbox" style="background-image:url({{image}});"></div>
                {{/if}}
                <p><strong>{{name}}</strong><br><br>{{bio}}<br><br> <a href="{{url}}">More by {{name}}</a></p>
              {{/author}}
            </div>
        </article>
    </div>
</main>

{{/post}}
